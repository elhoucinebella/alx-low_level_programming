#include<stdio.h>
/**
 * main - Entry point
 * Description: a program that prints numbers
 * Return: 0 always (uccess)
 */
int main(void)
{
	int Number = 0;

	for (Number = 0; Number <= 10; Number++)
	{
	printf("%d", Number);
	}
	{
	putchar('\n');
	}

return (0);
}
