#include "main.h"
#include <stdio.h>
/**
 * print_sign - prints signs
 * @n: parameter
 * Return: 1 and 0.
 */
int print_sign(int n)
{
	if (n > 0)
	{
		_putchar(53);
	}
	return (1);

	if (n == 0)
	{
		_putchar(48);
	}
	return (0);

		else 
		{
			_putchar(95);
		}
			return (-1);
}
